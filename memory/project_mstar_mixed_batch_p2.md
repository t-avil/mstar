---
name: project_mstar_mixed_batch_p2
description: W5-P2 mixed prefill+decode captured batch — code-verified design facts for exp/mixed-batch-p2
metadata: 
  node_type: memory
  type: project
  originSessionId: e01b1ff5-c6b6-4e43-a2ed-40135d9fe641
---

W5-P2 mixed CAPTURED batch (MSTAR_MIXED_BATCH, default OFF) in worktree /m-coriander/coriander/tim/mstar-p2, branch exp/mixed-batch-p2, base 23d07ee (has P1 chunked prefill). Files are under the worktree; cache_manager is mstar/engine/cache_manager.py (NOT worker/), submodule is mstar/model/qwen3_omni/submodules.py. No GPU runs. Related: [[project_mstar_vllm_decode_gap]].

CODE-VERIFIED FACTS (re-verify before relying):
- ScheduledBatch.graph_walk is ONE per-batch value (micro_scheduler.py:24-31); _select_node_priority (97-103) enforces same walk for whole batch. But per-request CurrentForwardPassInfo.graph_walk EXISTS per request via get_fwd_info. So mixed batch = batch.graph_walk="thinker_mixed" for config/runner dispatch, but each request keeps its OWN walk (thinker_decode / prefill_text) that prepare_inputs and postprocess must dispatch on.
- prepare_inputs called with graph_walk=batch.graph_walk for EVERY req (kv_cache_engine.py:826). For mixed, must derive per-req effective walk from fwd_info.graph_walk. prepare_inputs_batched (decode fast path) returns None for non-thinker_decode so mixed falls through to per-req loop — good.
- preprocess (submodules.py:904) already concats input_embeds/custom_pos_ids across reqs, builds seq_lens=[inp.input_seq_len], calls plan_attention(seq_lens,is_causal=True). Mixed = [1]*n+[C] falls out IF inputs list is [n decode ARNodeInputs, 1 chunk ARNodeInputs]. This is the golden reuse.
- plan_attention (cache_manager.py:337) is_decode=all(sl==1) -> mixed takes PREFILL wrapper automatically. kv_cache_locations built only for sl==1 rows (302-303) so mixed -> None -> prefill path. flashinfer_utils.py plan() (211-256) per-token KV scatter is length-agnostic, derives abs KV pos from qo_indptr lens + paged_kv_last_page_len per req. Correct for decode(append 1)+chunk(append C).
- Runner dispatch: run() (cuda_graph_runner.py:1134) -> _get_key_for(bs,num_tokens=sum seq_lens,walk) -> config.get_config_type() -> _run_flashinfer_packed (1431) for FLASH_INFER_PACKED. num_tokens=n+C. _run_flashinfer_packed pads with zero-len inputs to padded_bs, runs submodule.preprocess (plan_attention), copies packed into static bufs, replays, advances, samples. GENERIC — mixed just needs preprocess to produce [1]*n+[C].
- Sampling: _sample_and_remap (1742) fast path samples batched_logits[:len(rids)] = qo_indptr[1:]-1 last-token per req = n+1 rows, new_token per rid. postprocess (submodules.py:1529) return_token = graph_walk=="thinker_decode" OR is_last_prefill; drops new_token otherwise. So decode rids keep token, chunk rid drops unless last chunk — AUTOMATIC via per-req graph_walk. Gate mixed OFF when applied_penalty_in_graph (E10 gate mirror).
- CAPTURE seq_lens: _make_dummy_seq_lens (cuda_graph_runner.py:460) splits total evenly [total//bs]*bs. Differs from replay [1]*n+[C] but OK: attention metadata (qo_indptr/token scatter) re-planned OUTSIDE graph at replay via .copy_() into static bufs; capture only fixes kernel launches+buffer addresses. Same as existing prefill capture. padded_bs constant=32, qo_indptr len 33 both paths.
- Config: register FlashInferPackedCudaGraphConfig capture_graph_walk="thinker_mixed", replay_graph_walks=["thinker_mixed"], packed_seq_len_to_inputs reuse _build_prefill_text_packed(num_tokens). Two buckets: (bs=32, num_tokens=288=32+256), (bs=32, num_tokens=544=32+512). NOTE bs=32 total rows = n decode + 1 chunk, so n=31 for the 288 bucket etc.
- Worker loop: thinker_mixed is NOT a spec-continuable decode; _try_speculate_next yields None for non-thinker_decode walk -> lands non-spec path (worker.py:3008-3065). No spec-chain integration needed (P3).
- Flag helper: _envflag in qwen3_omni_model.py:86. P1 chunk planner plan_prefill_chunk (170-212), chunk metadata set in _get_thinker_prefill_forward (~1046) and _get_thinker_forward (~1517).
- PENALTY CORRUPTION (design D): sampling.py Sampler.sample (344) at line 411-412 adds ALL sampled tokens (incl discarded chunk-row sample) to _seen_token_mask when any_rep_pen (repetition_penalty!=1.0); line 415 advances RNG _step_offset per rid. So a NON-last chunk row with rep_pen>1 corrupts its seen mask + RNG. GATE: at scheduler assembly, only mix a chunk request whose repetition_penalty==1.0. Decode rows unaffected (they keep their token). packed path applies penalty OUTSIDE graph (_sample_and_remap line 1786), so mixed applied_penalty_in_graph=False and the in-graph staging is a no-op.
- postprocess split is AUTOMATIC: postprocess_batch (kv_cache_engine.py:890) iterates per_request_info, passes each rid's OWN info to postprocess; submodules.py postprocess return_token = graph_walk=="thinker_decode" OR is_last_prefill. So decode rids keep new_token, chunk rid drops unless last. check_stop same. NO code change needed in postprocess/check_stop.
- IMPLEMENTED + COMMITTED (2 commits on exp/mixed-batch-p2, base 23d07ee): (1) 1093527 flags + thinker_mixed graph config (flag-gated) + submodule plumbing; (2) b265cd1 scheduler assembly + non-spec dispatch + validation hooks. Files: qwen3_omni_model.py (flags), submodules.py (MIXED_BATCH_BS=32/CHUNK_SIZES=[256,512], config, prepare_inputs delegates thinker_mixed->fwd_info.graph_walk, forward_batched is_prefill+=thinker_mixed, preprocess DEBUG log+assert), micro_scheduler.py (_try_assemble_mixed/_pop_mixed_batch), worker.py (_can_speculate False for thinker_mixed). VERIFIED (no GPU): flag helpers, scheduler assembly 6 gate cases + caps, config enumeration flag on/off (buckets [288,544] bs=[32]), P1 tests 13 pass flag-off, import/wiring sanity. Env for CPU tests: PYTHONPATH=/m-coriander/coriander/tim/mstar-p2 CUDA_VISIBLE_DEVICES="" /m-coriander/coriander/tim/mstar-new/.venv/bin/python (p2 worktree has NO .venv; borrow sibling's).
- RESIDUAL GPU-TEST RISKS: (a) capture seq_lens _make_dummy_seq_lens(32,288)=[9]*32 differs from replay [1]*31+[257] — reasoned safe (attn metadata re-planned outside graph) but unproven on GPU; (b) actual mixed forward numerics (decode+chunk in one FlashInfer prefill call) never run; (c) closed-loop that decode+chunk are simultaneously ready often enough to trigger assembly. Need GPU A/B: flag-off vs flag-on SEMANTICALLY-identical decode (NOT bitwise — different fp8 tiles/split-KV per span diverge after a few tokens per team lead; assert seq_len/position landings + quality/first-token) + faster JCT.
- CRITICAL FIX (commit 7c09d10): worker.py postprocess routed outputs by BATCH walk (batch_N.graph_walk). For thinker_mixed that key doesn't exist in walk_node_to_worker_graph_id[(walk,next_node)] -> loop-back edges treated external -> SILENT HANG. Fixed: resolve effective_walk = per_request_info[rid].graph_walk when batch walk=="thinker_mixed", use for store_and_populate_graph_edges AND process_node_outputs (worker.py ~2196-2238). Same class as base 23d07ee's missing-edge hang. Also: PendingLoopStop recorded under per-req walk; MicroScheduler got tp_nodes param + skips mixed on TP nodes (thinker_mixed TP has no sharding group -> None._workers AttributeError). Mixed skips fast prem new-token emit (thinker_decode-only) — correct but unoptimized (P3).
- P1 ALTERNATION GPU-REJECTED (team lead, live): i2t B32 0.82x, s2t B8 0.73x — per-chunk conductor round-trip costs more than the stall removed. So P2 mixed is the SOLE path. LOAD-BEARING: chunk row MUST ride inside decode step's single worker submission = zero extra conductor round-trips (my design does this). P1 chunk plumbing now live-validated (multi-chunk offsets, asserts green).
- AUDIT DONE: all batch_N.graph_walk uses in worker.py postprocess path checked. Safe as-is (batch-level telemetry line 2314 register_end, TP fan-out gated off, spec-overstay only for speculative which mixed isn't). Only store+route needed per-req walk.
- P2 chunk restricted to prefill_text ONLY (mixed capture is text-signature: input_embeds+cos+sin). Audio needs mrope_pos_advance side-channel, vision needs deepstack — both P3.
