"""Pi0.5 components package — top-level classes live in
``paligemma.py`` and ``action_expert.py``. The shared transformer
building blocks (RMSNorm, GatedMLP, Attention, DecoderLayer) come from
``mminf.model.components``.
"""
